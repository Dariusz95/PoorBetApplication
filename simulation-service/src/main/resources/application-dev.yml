spring:
  application:
    name: simulationservice

poorbet:
  security:
    enabled: true
    issuer: ${AUTH_ISSUER_URI}
    internal-audience:
      - simulation-service
    jwk-set-uri: ${AUTH_JWKS_URI}
